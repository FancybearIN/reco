Place the architecture sketch image here as `reco-sketch.png`.

If you attached an image in the chat, save it to this folder with the filename `reco-sketch.png` so it can be displayed in the project `README.md`.

Example:

```bash
# from where you downloaded the attachment
mv ~/Downloads/reco-sketch.png docs/images/reco-sketch.png
```